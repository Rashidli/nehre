import { toast } from "react-toastify";
// import i18n from 'i18next';

export const successToast = (key) => toast.success(key);
export const infoToast = (key) => toast.info(key);
export const warningToast = (key) => toast.warn(key);
export const errorToast = (key) => toast.error(key);



